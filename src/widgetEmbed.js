
import StylaWidget from './widget';

let config = {
    slug    : 'braunhamburg',
    domain  : 'http://www.braun-hamburg.de/stories/',
    domainConfigAPI : 'https://www.amazine.com/api/config/'
};

let widget = window.widget = new StylaWidget( config );
